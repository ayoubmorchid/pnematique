# Pneumatique React Store

Pneumatique is a React + Vite storefront for automotive parts and accessories. It includes a home page, product catalog, cart flow, simple protected checkout pages, and invoice export utilities.

## Features

- React single-page application structure
- Client-side routing
- Animated UI with Framer Motion
- Icon-based interface elements
- PDF/image export utilities with `jspdf` and `html2canvas`
- Styling workflow with Tailwind CSS and PostCSS
- Responsive layout for desktop and mobile screens

## Tech Stack

- React 18
- Vite
- React Router
- Tailwind CSS
- Framer Motion
- Lucide React
- Font Awesome
- jsPDF
- html2canvas

## Local Setup

Install dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

Build for production:

```bash
npm run build
```

Preview the build:

```bash
npm run preview
```

## What I Practiced

- Building a React/Vite frontend project
- Working with routing and animated UI states
- Organizing frontend dependencies
- Preparing a more polished user interface

## Status

Frontend learning project. Before pinning, add screenshots, a clearer project name, and a short explanation of the real use case.
